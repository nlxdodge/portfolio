<header>
    <nav>
        <a class="{{ $page->selected('') }}" href="/"><i class="fas fa-kiwi-bird"></i></a>
        <a class="{{ $page->selected('works') }}" href="{{ $page->baseUrl }}/works">Works</a>
        <a class="{{ $page->selected('posts') }}" href="{{ $page->baseUrl }}/posts">Posts</a>
        <a href="#" class="toggle-colors"><i class="far fa-moon"></i></a>
    </nav>
</header>
